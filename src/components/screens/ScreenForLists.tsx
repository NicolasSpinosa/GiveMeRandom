import { View } from "react-native";
import { appStyles } from "../../styles/appStyles";
import { Text } from "../generics/Text";

export const ScreenForLists = () => (
    <View style={appStyles.screen}>
        <Text>Lists</Text>
    </View>
);